import React from 'react';
import { View, Text } from 'react-native';
import Btn from './Btn';

const acadDetails = (props) => {
        return(

                <Text>{ props.children }</Text>

        );
}

export default acadDetails;